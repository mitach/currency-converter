{
  "name": "currency-converter",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && node dist/server.js",
    "heroku-postbuild": "cd backend && npm install && npx tsc"
  }
} 