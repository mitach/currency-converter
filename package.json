{
  "name": "currency-converter",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "cd backend && node dist/server.js",
    "heroku-postbuild": "cd backend && npm install && npm install -g typescript && tsc"
  }
} 